const showFarewellMessage = (username) => {
  const farewellMessage = `\nThank you for using File Manager, ${username}, goodbye!`;
  console.log(farewellMessage);
};

export default showFarewellMessage;
